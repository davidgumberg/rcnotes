<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<%= link_to "Add a category", new_category_path %>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
  <% end %>
</div>

